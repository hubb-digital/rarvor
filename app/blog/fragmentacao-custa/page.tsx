import type { Metadata } from "next";
import { ArticlePage } from "../../section-pages";
export const metadata: Metadata = { title: "A fragmentação custa mais do que aparece — RARVOR", description: "Quando cada fornecedor enxerga apenas a própria entrega, ninguém responde pelo todo." };
export default function Page(){ return <ArticlePage lang="pt" slug="fragmentacao-custa" />; }
