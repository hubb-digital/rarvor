import type { Metadata } from "next"; import { SolutionsPage } from "../../section-pages";
export const metadata:Metadata={title:"Strategy, growth and technology solutions"}; export default function Page(){return <SolutionsPage lang="en"/>}
