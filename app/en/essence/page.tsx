import type { Metadata } from "next";
import { EssencePage } from "../../section-pages";
export const metadata: Metadata = { title: "Essence — RARVOR", description: "RARVOR's essence, method and operating architecture." };
export default function Page(){ return <EssencePage lang="en" />; }
