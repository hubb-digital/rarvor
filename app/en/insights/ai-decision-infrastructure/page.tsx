import type { Metadata } from "next";
import { ArticlePage } from "../../../section-pages";
export const metadata: Metadata = { title: "AI is decision infrastructure — RARVOR", description: "AI's value lies in preserving context and improving judgment." };
export default function Page(){ return <ArticlePage lang="en" slug="ai-decision-infrastructure" />; }
