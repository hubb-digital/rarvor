import type { Metadata } from "next";
import { ArticlePage } from "../../../section-pages";
export const metadata: Metadata = { title: "Fragmentation costs more than it shows — RARVOR", description: "When every supplier sees only its own delivery, no one is responsible for the whole." };
export default function Page(){ return <ArticlePage lang="en" slug="fragmentation-cost" />; }
